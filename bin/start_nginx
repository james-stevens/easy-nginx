#! /bin/sh

cp -a /usr/local/nginx/ /run
mkdir /run/nginx/ca
chmod 700 /run/nginx
chown -R nginx: /run/nginx

/usr/local/bin/update_pems
/usr/local/bin/remake_conf

/usr/sbin/nginx -t -c /run/nginx/nginx.conf

exec /usr/sbin/nginx -c /run/nginx/nginx.conf -g "daemon off;" >/tmp/nginx.log 2>&1
